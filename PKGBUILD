# Maintainer iFlygo <iflygo@outlook.com>

pkgbase='iflygo'
pkgname=(
	'iflygo-base'
)
pkgver=0.1.0
pkgrel=1
pkgdesc="Packages installed on iFlygo's Arch Linux"
arch=('any')
url='https://github.com/colorsakura/dotfiles'
license=('None')

package_iflygo-base() {
	depends=(
		# base
		'base'
		'btrfs-progs'
		'dash'
		'linux-firmware'
		'man-pages'
        'fish'

		# base-devel
		'base-devel'
		'posix'
		'autoconf'
		'automake'
		'bison'
		'fakeroot'
		'flex'
		'gcc'
		'gettext'
		'groff'
		'gzip'
		'libtool'
		'make'
		'pacman'
		'pkgconf'
		'sudo'
		'texinfo'
		'which'

		# other arch
		'pacman-contrib'
		'pkgstats'

		# files
		'bat'
		'exa'
		'fd'
		'ripgrep'
		'tree'
		'imagemagick'
		'jq'
		'inotify-tools'

		# archiving
		'7-zip'
		'unrar'
		'unzip'
		'zip'

		# network
		'bandwhich'
		'bind'
		'dog'
		'iwd'
		'net-tools'
		'nftables'
		'openbsd-netcat'
		'openssh'
		'rsync'
		'socat'
		'wget'
        'curl'

		# security
		'arch-audit'
		'ccid'
		'pam-u2f'
		'usbguard'
		'pinentry'
		'gcr'
		'checksec'
		'polkit-gnome'
		'mokutil'

		# hardware
		'acpi'
		'bolt'
		'dmidecode'
		'fwupd'
		'throttled'
		'tlp'
		'upower'

		# audio
		'bluez'
		'bluez-utils'
		'helvum'
		'pipewire-pulse'
		'playerctl'
		'pulseaudio-alsa'
        'pipewire'

		# dw
		'light'
		'python-i3ipc'
		'qt5-wayland'
		'qt6-wayland'
		'slurp'
		'swaybg'
		'swayidle'
		'swaylock'
		'waybar'
		'wl-clipboard'
		'xorg-server-xwayland'

		# fonts
		'noto-fonts'
		'noto-fonts-cjk'

		# dev tools
		'fzf'
		'github-cli'
        'git'

		# shell dev
		'bash-language-server'
		'checkbashisms'
		'shfmt'
		'bash-completion'

		# python dev
		'python-black'
		'python-pip'

		# go dev
		'go'
		'go-tools'
		'gopls'
		'revive'
		'staticcheck'

		# js dev
		'npm'
		'yarn'
		'typescript-language-server'

		# rust dev
		'rust'
		'rust-analyzer'

		# c dev
		'meson'

		# terminal
        'kitty'
		'alacritty'

		# passwords
		'gnome-keyring'
		'libgnome-keyring'

		# browsers
		'firefox'

		# screenshots & gifs & desktop sharing
		'grim'
		'swappy'
		'wf-recorder'

		# media
		'xdg-desktop-portal-wlr'
		'wireplumber'

		# video
		'mpv'
		'ffmpeg'
		'yt-dlp'
		'aria2'

		# misc
	)
}
